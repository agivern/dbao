{% extends 'DBAOAdminBundle::layout.html.twig' %}

{# Déclaration du titre de la page, imprort des JS/CSS #}
{% block title %}{{ parent() }} - clients{% endblock %}

{% block import %}
    <link rel="stylesheet" href="{{ asset('bundles/dbaoadmin/css/clients.css') }}" type="text/css" media="all" />
{% endblock %}

{# Corps de la page #}
{% block body %}

    <div id="titre_rubrique">CLIENT</div>

    {% set i = 0 %}
    {% for c in client %}
        {% set i = i + 1 %}
        {% if i % 8 == 0 %}
            <div id="client" style="margin-right: 0px;">
                <img id="client_image" src="/dbao_light_site/web/uploads/documents/{{ c.url }}"/>
                <form action="{{ path('dbao_admin_clients_modifier', {'id': c.id}) }}" method="post">
                    <input type="text" name="form_id" value="{{ c.emplacement }}" style="width:25px"/>
                    <input type="submit">
                </form>
                <a id="lien_acces" href="{{ path('dbao_admin_clients_supprimer', {'id': c.id}) }}" onclick="return confirm('Etes vous sûre de vouloir supprimer cet élément ?');">Supprimer <p>></p></a>
            </div>
        {% else %}
            <div id="client">
                <img id="client_image" src="/dbao_light_site/web/uploads/documents/{{ c.url }}"/>
                <form action="{{ path('dbao_admin_clients_modifier', {'id': c.id}) }}" method="post">
                    <input type="text" name="form_id" value="{{ c.emplacement }}" style="width:25px"/>
                    <input type="submit">
                </form>
                <a id="lien_acces" href="{{ path('dbao_admin_clients_supprimer', {'id': c.id}) }}" onclick="return confirm('Etes vous sûre de vouloir supprimer cet élément ?');">Supprimer <p>></p></a>
            </div>
        {% endif %}
    {% endfor %}

    <div class="clear"></div>
    <center><a href="{{ path('dbao_admin_clients_edition') }}">Ajouter un client</a></center>
{% endblock %}

