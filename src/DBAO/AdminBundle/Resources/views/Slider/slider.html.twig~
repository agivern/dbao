{% extends 'DBAOAdminBundle::layout.html.twig' %}

{# Déclaration du titre de la page, imprort des JS/CSS #}
{% block title %}{{ parent() }} - slider{% endblock %}

{% block import %}
    <link rel="stylesheet" href="{{ asset('bundles/dbaoadmin/css/slider.css') }}" type="text/css" media="all" />
{% endblock %}

{# Corps de la page #}
{% block body %}
    <div id="titre_rubrique">SLIDER</div>

    {% for s in slider %}
        <div id="slide">
            <img id="slide" class="GrandeImage" src="/dbao_light_site/web/uploads/documents/{{ s.bigUrl }}">
            </img>
            <div id="slide" class="titre">
                {{ s.titre }}
            </div>
            <form action="{{ path('dbao_admin_slider_update', {'id': s.id}) }}" method="post">
                <input type="text" name="form_id" value="{{ s.emplacement }}"/>
                <input type="submit">
            </form>
            <a id="lien_acces" href="{{ path('dbao_admin_slider_delete', {'id': s.id}) }}">Supprimer <p>></p></a>
        </div>
    {% endfor %}

    <div class="clear"></div>
    <center><a href="{{ path('dbao_admin_slider_edition') }}"> Ajouter un slider </a></center>
{% endblock %}

