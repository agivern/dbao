{% extends 'DBAOSiteBundle::layout.html.twig' %}

{# Déclaration du titre de la page, imprort des JS/CSS #}
{% block title %}{{ parent() }} - Accueil{% endblock %}

{% block import %}
    <link rel="stylesheet" href="{{ asset('bundles/dbaosite/css/accueil.css') }}" type="text/css" media="all" />
{% endblock %}

{# Corps de la page #}
{% block body %}

    <div id="slider">
        <div id="mask">
            <ul>
            {% set i = 0 %}
            {% for s in slider %}
                {% set i = i + 1 %}
                {% if i == 1 %}
                    <li id="first" class="firstanimation">
                {% elseif i == 2 %}
                    <li id="second" class="secondanimation">
                {% elseif i == 3 %}
                    <li id="third" class="thirdanimation">
                {% elseif i == 4 %}
                    <li id="fourth" class="fourthanimation">
                {% else %}
                    <li id="fifth" class="fifthanimation">
                {% endif %}
                        <img src="/dbao_light_site/web/uploads/documents/{{ s.bigUrl }}" alt="{{ s.titre }}"/>
                        <div class="slideTitre">{{ s.titre }}</div>
                    </li>
            {% endfor %}
            </ul>
        </div>
        <div class="progress-bar"></div>
    </div>

    <div id="minSlider">
        <div id="mask">
            <ul>
            {% set i = 0 %}
            {% for s in slider %}
                {% set i = i + 1 %}
                {% if i == 1 %}
                    <li id="first" class="firstanimation">
                {% elseif i == 2 %}
                    <li id="second" class="secondanimation">
                {% elseif i == 3 %}
                    <li id="third" class="thirdanimation">
                {% elseif i == 4 %}
                    <li id="fourth" class="fourthanimation">
                {% else %}
                    <li id="fifth" class="fifthanimation">
                {% endif %}
                        <img src="/dbao_light_site/web/uploads/documents/{{ s.littleUrl }}" alt="{{ s.titre }}"/>
                        <div class="slideTitre">{{ s.titre }}</div>
                    </li>
            {% endfor %}
            </ul>
        </div>
        <div class="progress-bar"></div>
    </div>

    <div id="titre_rubrique">
        BOOK</br>
        <a id="lien_acces" href="{{ path('dbao_site_realisations') }}">Découvrir le book <p>></p></a>
    </div>

    <div class="realisationRubrique">
    {% set i = 0 %}
    {% for r in realisation %}
    {% if r.first %}
        {% set i = i + 1 %}
        <a href="{{ path ('dbao_site_realisations_contenu', { 'id': r.id }) }}">
        {% if i == 1 %}
            <div id="realisation" class="gauche">
        {% elseif i == 2 %}
            <div id="realisation" class="gauche" style="margin-right: 0px;">
        {% elseif i == 3 %}
            <div id="realisation" class="droite">
        {% else %}
            <div id="realisation" class="droite" style="margin-left: 10px;">
        {% endif %}
                <img id="rubrique_image" src="/dbao_light_site/web/uploads/documents/{{ r.url }}"></img>
                <div id="rubrique_titre">{{ r.titre }}</div>
                <div class="realisationAccroche">{{ r.accroche }}</div>
            </div>
        </a>
    {% endif %}
    {% endfor %}
    </div>

    <div id="titre_rubrique">
        EXPERTISE</br>
        <a id="lien_acces" href="{{ path('dbao_site_expertise') }}">Visiter la page <p>></p></a>
    </div>

    <div class="expertiseRubrique">
        <div id="expertise">
            <div id="rubrique_titre">Stratégie</div>

            <div class="expertiseCadre">
                <div id="logo" class="strategie"></div>

                <p class="accrocheExpertise">
                    Nous élaborons</br>
                    des univers cohérents...
                </p>

                <p class="texteExpertise">
                    ... en adéquation avec vous, avec votre
                    cible, pour une meilleure connivence entre
                    eux, et vous !
                </p>
            </div>
            <p class="motCleExpertise">Identité</p>
            <p class="motCleExpertise">Positionnement</p>
            <p class="motCleExpertise">Diffusion</p>
        </div>

        <div id="expertise" class="particulier">
            <div id="rubrique_titre">Design</div>

            <div class="expertiseCadre">
                <div id="logo" class="design"></div>

                <p class="accrocheExpertise">
                   Ecouter vos mots,</br>
                   pour communiquer sans !
                </p>

                <p class="texteExpertise">
                    Nous utilisons le design comme un outil
                    purement fonctionnel : la forme au  service
                    du fond, pour valoriser le message.
                </p>
            </div>

            <p class="motCleExpertise">Design Print</p>
            <p class="motCleExpertise">Webdesign</p>
            <p class="motCleExpertise">Aménagement de volumes</p>
        </div>

        <div id="expertise">
            <div id="rubrique_titre">Développement</div>

            <div class="expertiseCadre">
                <div id="logo" class="developpement"></div>

                <p class="accrocheExpertise">
                    L'architecture digitale</br>
                    multi-devices !
                </p>

                <p class="texteExpertise">
                    Nous maîtrisons aussi bien les toutes
                    dernières technologies, que l'ensemble
                    des problématiques liées au web.
                </p>
            </div>

            <p class="motCleExpertise">Back office</p>
            <p class="motCleExpertise">Référencement naturel</p>
            <p class="motCleExpertise">Hébergement</p>
        </div>

        <div id="expertise" style="margin-right: 0px; margin-bottom: 0px;">
            <div id="rubrique_titre">3D / Motion design</div>

            <div class="expertiseCadre">
                <div id="logo" class="motion"></div>

                <p class="accrocheExpertise">
                    Show the real...</br>
                    with the virtual !
                </p>

                <p class="texteExpertise">
                    Créez une immersion optimale, générez
                    une démarche participative en rendant
                    votre support interactif.
                </p>
            </div>

            <p class="motCleExpertise">3D</p>
            <p class="motCleExpertise">Animation</p>
            <p class="motCleExpertise">Motion design</p>
        </div>
    </div>

    <div id="titre_rubrique">
        NEWS</br>
        <a id="lien_acces" href="{{ path('dbao_site_news') }}">Accédez a toutes <p>></p></a>
    </div>

    <div id="news_rubrique">
        {% set i = 0 %}
        {% for n in news %}
            {% set i = i + 1 %}
            {% set j = i % 4 %}
            {% if j == 1 %}
                <div id="news">
            {% elseif j == 2 %}
                <div id="news" class="deux">
            {% elseif j == 3 %}
                <div id="news" class="particulier">
            {% else %}
                <div id="news" class="particulier quatre">
            {% endif %}
                    {% for img in images %}
                        {% if img.news == n and img.emplacement == 1 %}
                            <img id="rubrique_image" src="/dbao_light_site/web/uploads/documents/{{ img.url }}"></img>
                        {% endif %}
                    {% endfor %}
                    <p class="newsDescription">{{ n.description }}</p>
                    <a id="lien_acces" class="news" href="{{ path('dbao_site_news_redi', { 'name': n.titre}) }}">Lire la news <p>></p></a>
                </div>
        {% endfor %}
    </div>

{% endblock %}

